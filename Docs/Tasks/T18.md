# T18 — Integration tests

- **Phase:** 3 · Test/CI
- **Phụ thuộc:** T06, T13

## Bối cảnh
Cần test Service + tầng DB thật để chắc query/transaction đúng sau refactor.

## Mục tiêu
- Thiết lập DB test: **SQLite in-memory** hoặc DB MySQL riêng cho test; chạy migrations trước mỗi suite.
- Viết integration test cho các Service chính: `ProductService` (CRUD + search + pagination), `OrderService` (tạo đơn, đếm, doanh thu), `CartService`, `UserService`.
- Test transaction: thất bại giữa chừng → rollback đúng.

## Files
- `tests/Integration/...`, helper bootstrap DB test

## Done (acceptance)
- Integration suite xanh; CRUD + search + transaction kiểm chứng được.

## Verify
Theo RULE trong [`CLAUDE.md`](../../CLAUDE.md). Bổ sung: đảm bảo test cô lập (mỗi test tự dọn dữ liệu / dùng transaction rollback).

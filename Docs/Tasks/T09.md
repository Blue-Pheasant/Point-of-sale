# T09 — Validator hợp nhất *(bỏ 3 bản trùng)*

- **Phase:** 1 · Core
- **Phụ thuộc:** T03
- **Status:** ✅ Done (2026-05-29) — verify PASS, `php -l`/PHPStan/PHPUnit xanh.

## Bối cảnh
Logic validate lặp ở **3 nơi** với **2 cú pháp khác nhau**:
- `Core/Model.php` (L128) — rule dạng mảng `[self::RULE_MIN, 'min'=>5]`; có check `RULE_REQUIRED` **trùng** (L137 & L152); `RULE_NUMBER` định nghĩa nhưng không xử lý.
- `Core/UserModel.php` (L24, `validateUpdateProfile`) — copy gần như y hệt vòng lặp của Model.
- `Core/FormRequest.php` (L141) — rule dạng chuỗi `'min:5'`; **bug**: `__construct` gọi `array_merge($this->getPrams(), $this->getBody())` nhưng **không gán** → `$this->data` luôn rỗng nên validate vô nghĩa.

## Mục tiêu
1. Tạo **một** engine validate (class/trait `Validator`) + **một** cú pháp rule thống nhất.
2. `Model`, `UserModel`, `FormRequest` đều delegate vào engine đó.
3. Xoá `UserModel::validateUpdateProfile` trùng (chuyển rule sang khai báo).
4. Sửa bug `$this->data` trong `FormRequest`.
5. Bỏ check `RULE_REQUIRED` trùng; bổ sung rule còn thiếu (`RULE_NUMBER`...).

## Files
- `Core/Model.php`, `Core/UserModel.php`, `Core/FormRequest.php` (+ `Core/Validator.php` mới)

## Done (acceptance)
- Validate đăng ký/cập nhật profile/sản phẩm báo lỗi đúng.
- `FormRequest` nhận đúng data (không còn rỗng).
- Có unit test cho từng rule (mở rộng ở T17).

## Verify
Theo RULE trong [`CLAUDE.md`](../../CLAUDE.md). Bổ sung: test các rule chính (required/email/min/max/match/number/unique).

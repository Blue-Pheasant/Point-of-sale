# T21 — Báo cáo & analytics

- **Phase:** 4 · Feature
- **Phụ thuộc:** T05
- **Status:** ✅ Done (2026-05-30) — verify PASS, `php -l`/PHPStan/PHPUnit xanh.

## Bối cảnh
Dashboard (`Controllers/AdminController.php`) hiện chỉ có tổng số (orders/products/users/income), chưa có dữ liệu theo thời gian.

## Mục tiêu
1. Mở rộng `Services/OrderService` (qua Query Builder): doanh thu theo ngày/tháng (`GROUP BY DATE(created_at)`), top sản phẩm bán chạy (join `order_items`), giá trị đơn trung bình (AOV).
2. Dashboard admin: thêm biểu đồ (Chart.js qua CDN) cho doanh thu + top sản phẩm.

## Files
- `Services/OrderService.php`, `Controllers/AdminController.php`, `views/admin/dashboard.php`

## Done (acceptance)
- Dashboard hiển thị biểu đồ doanh thu theo thời gian + top sản phẩm.
- Số liệu khớp dữ liệu DB.

## Verify
Theo RULE trong [`CLAUDE.md`](../../CLAUDE.md). Bổ sung: đối chiếu vài con số với truy vấn tay; query analytics param-bound.

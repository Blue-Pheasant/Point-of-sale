# T23 — REST API + filter tìm kiếm

- **Phase:** 4 · Feature
- **Phụ thuộc:** T07, T08, T13
- **Status:** ✅ Done (2026-05-30) — verify PASS (gồm security review filter param-bound), `php -l`/PHPStan/PHPUnit xanh.

## Bối cảnh
Hệ thống chỉ render server-side, chưa có API JSON. Search sản phẩm chỉ theo keyword (và đang lỗi — xem T13).

## Mục tiêu
1. `Controllers/Api/*` + base `ApiController` trả JSON qua `Response::json()` (T08); route group `/api/*` (T07) với verb đầy đủ + middleware token (auth API tách khỏi session cookie).
2. Gộp logic search/filter thành **một** `ProductService::searchProducts($filters, $pager)` — keyword + category + khoảng giá — dùng Query Builder param-bound; thay thế `findProductByKeyWord` (đã xử lý ở T13).
3. Endpoint mẫu: `GET /api/products?category=&min_price=&max_price=&q=&page=`.

## Files
- `Controllers/Api/` (+ `ApiController`), `Routes/` (group api), `Services/ProductService.php`, middleware token

## Done (acceptance)
- `GET /api/products?...` trả JSON đúng (data + pagination), filter hoạt động.
- API có phân trang; lỗi trả JSON + status đúng.

## Verify
Theo RULE trong [`CLAUDE.md`](../../CLAUDE.md). Bổ sung: test filter kết hợp; thử inject vào query params → param-bound chặn.

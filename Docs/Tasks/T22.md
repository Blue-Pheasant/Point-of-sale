# T22 — Thanh toán thật

- **Phase:** 4 · Feature
- **Phụ thuộc:** T07, T20

## Bối cảnh
`payment_method` hiện chỉ là chuỗi, không có tích hợp cổng thanh toán nào.

## Mục tiêu
1. Thiết kế `Services/Payment/PaymentGatewayInterface` (vd `createPayment`, `verifyCallback`).
2. Một driver **sandbox/test** (VNPay/Momo/Stripe test) hiện thực interface.
3. Lưu `payment_status` + `transaction_id` trên bảng `orders` (migration nếu cần).
4. Route callback/return (verb PUT/POST từ T07) cập nhật trạng thái đơn.

> Mục tiêu portfolio: thể hiện **abstraction sạch** (đổi gateway không sửa nghiệp vụ), dùng key sandbox — không cần lên production thật.

## Files
- `Services/Payment/` (interface + driver), `Models/Order.php`, migration (nếu cần), `Routes/OrderRoute.php`, `Controllers/OrderController.php`

## Done (acceptance)
- Tạo thanh toán sandbox → nhận callback → `payment_status` cập nhật đúng.
- Có thể thêm gateway mới chỉ bằng cách implement interface.

## Verify
Theo RULE trong [`CLAUDE.md`](../../CLAUDE.md). Bổ sung: verify chữ ký/callback an toàn; không hardcode secret (đọc từ env).

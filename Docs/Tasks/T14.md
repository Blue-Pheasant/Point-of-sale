# T14 — Gỡ trùng lặp & magic numbers

- **Phase:** 2 · Quality
- **Phụ thuộc:** T06
- **Status:** ✅ Done (2026-05-29) — verify PASS, `php -l`/PHPStan/PHPUnit xanh.

## Bối cảnh & Mục tiêu
1. **Giá theo size** `+3000 / +6000` hardcode lặp ở `Models/CartItem.php` (L59-62) và `Services/OrderService.php` (L153-158) → tách thành constants hoặc `Services/PricingService` dùng chung.
2. **Status strings** `'processing'/'done'/'cancel'/'accepted'` rải rác trong query/code → gom vào hằng số `Models/Order::STATUS_*`.
3. **Pattern pagination** lặp gần như y hệt ở `ProductService`/`UserService`/`OrderService` (`getAll*`) → tách helper chung tái dùng `Common/Pagination.php`.

## Files
- `Models/CartItem.php`, `Services/OrderService.php`, `Models/Order.php`, `Services/ProductService.php`, `Services/UserService.php` (+ `Services/PricingService.php` nếu tạo)

## Done (acceptance)
- Không còn số 3000/6000 lặp; tính giá qua một nguồn.
- Status dùng hằng số thay vì chuỗi rời.
- Pagination dùng một helper.

## Verify
Theo RULE trong [`CLAUDE.md`](../../CLAUDE.md). Bổ sung: tính giá Medium/Large vẫn đúng; danh sách phân trang đúng.

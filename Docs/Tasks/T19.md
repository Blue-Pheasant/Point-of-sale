# T19 — GitHub Actions CI

- **Phase:** 3 · Test/CI
- **Phụ thuộc:** T03, T17

## Bối cảnh
`config.yml` (CircleCI) đã lỗi thời: image PHP 7.1, tham chiếu `package.json`/`yarn`/`codecept` không tồn tại. Không có pipeline chạy thật.

## Mục tiêu
1. Tạo `.github/workflows/ci.yml`: trigger trên `push` & `pull_request`; PHP 8.1; bước `composer install` → `composer lint` → `composer analyse` → `composer test`.
2. (Tùy chọn) dịch vụ MySQL trong workflow cho integration test (T18).
3. Xoá `config.yml` CircleCI cũ.

## Files
- `.github/workflows/ci.yml` (mới), xoá `config.yml`

## Done (acceptance)
- Mở PR → Actions chạy lint + analyse + test tự động và pass.

## Verify
Theo RULE trong [`CLAUDE.md`](../../CLAUDE.md). Bổ sung: tạo PR thử nghiệm để xác nhận workflow chạy.

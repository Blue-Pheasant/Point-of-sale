# T01 — Tạo CLAUDE.md + Rule verify

- **Phase:** Foundation
- **Phụ thuộc:** —
- **Status:** ✅ Done

## Bối cảnh
Cần một nơi mọi session tự đọc để biết kiến trúc, quy ước code, và quy trình verify bắt buộc.

## Mục tiêu
Tạo `CLAUDE.md` ở gốc repo gồm: tổng quan + cách chạy, bản đồ kiến trúc (theo code thực tế), quy ước code (PSR-12, SQL param-bound, escape output, migrate có alias `@deprecated`), và **RULE verify** + checklist 6 mục. Trỏ tới roadmap task.

## Files
- `CLAUDE.md` (mới)

## Done (acceptance)
- `CLAUDE.md` tồn tại ở gốc repo, có mục "RULE: Verify sau khi code" + checklist.
- Có bảng roadmap T01–T23.

## Verify
Theo RULE trong [`CLAUDE.md`](../../CLAUDE.md). Với task này: đọc lại file, đảm bảo rule rõ ràng và đúng đường dẫn.

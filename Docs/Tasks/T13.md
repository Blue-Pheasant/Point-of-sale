# T13 — Sửa bug xác nhận được

- **Phase:** 2 · Quality
- **Phụ thuộc:** T05, T09
- **Status:** ✅ Done (2026-05-29) — verify PASS, `php -l`/PHPStan/PHPUnit xanh.

## Bối cảnh & Mục tiêu
Ba bug đã xác nhận:
1. **`Services/ProductService.php` (L149-165, `findProductByKeyWord`)** — dùng biến `$products` **chưa khai báo** (đúng là `$req`) → **fatal error** khi search; đồng thời nối chuỗi `'%$keyword%'` (L154-155). → Viết lại thành `searchProducts()` dùng Query Builder param-bound (gắn với T23, có thể làm gọn ở đây trước).
2. **`Models/Product.php` & `Models/Category.php`** — lỗi cú pháp rule: `['min' >= 20]`, `['max' <= 30]` (so sánh boolean) → phải `[self::RULE_MIN, 'min' => 20]`, `[self::RULE_MAX, 'max' => 30]`.
3. **`Services/OrderService.php` (L125)** — đếm orders nhưng query `SELECT COUNT(*) FROM users` (sai bảng) → kiểm tra & sửa thành `orders`.

## Files
- `Services/ProductService.php`, `Models/Product.php`, `Models/Category.php`, `Services/OrderService.php`

## Done (acceptance)
- Search sản phẩm chạy không fatal, trả kết quả + pagination đúng.
- Rule product/category validate đúng.
- Số liệu đếm orders đúng.

## Verify
Theo RULE trong [`CLAUDE.md`](../../CLAUDE.md). Bổ sung: thử search keyword thật; tạo product thiếu field → báo lỗi đúng.

# T08 — Request / Response (web + API)

- **Phase:** 1 · Core
- **Phụ thuộc:** T07
- **Status:** ✅ Done (2026-05-29) — verify PASS, `php -l`/PHPStan/PHPUnit xanh.

## Bối cảnh
`Core/Request.php`: method tên sai `getPrams()` (typo), sanitize input lossy bằng `FILTER_SANITIZE_SPECIAL_CHARS` (mã hoá dữ liệu ngay lúc nhận — sai tầng), không parse JSON body. `Core/Response.php` quá mỏng: thiếu `json()`, typo `setStateCode`, `redirect()` **không `exit`** sau khi set header (code chạy tiếp), `back()` lỗi khi thiếu `HTTP_REFERER`; method lẫn lộn static/instance.

## Mục tiêu
**Request:** parse **JSON body** khi `Content-Type: application/json`; truy cập **route param** (từ T07); thêm `wantsJson()`; đổi `getPrams()→getParams()` (giữ alias cũ); **bỏ sanitize input lossy** (escape sẽ làm ở output — T11).
**Response:** thêm `json($data, $status)`; sửa `setStateCode→setStatusCode` (giữ alias); `redirect()` `exit` sau header; `back()` fallback khi thiếu referer; chuẩn hoá thành instance method nhất quán.

## Files
- `Core/Request.php`, `Core/Response.php`

## Done (acceptance)
- API trả JSON đúng header + status.
- Form/POST cũ vẫn chạy; redirect dừng thực thi đúng.

## Verify
Theo RULE trong [`CLAUDE.md`](../../CLAUDE.md). Bổ sung: kiểm các nơi gọi `getPrams`/`setStateCode` vẫn hoạt động qua alias.

# T17 — Unit tests

- **Phase:** 3 · Test/CI
- **Phụ thuộc:** T05, T07, T09, T14

## Bối cảnh
Repo hiện **0 test**. Phần logic thuần đã refactor là nơi test mang lại giá trị cao và dễ test.

## Mục tiêu
Viết unit test (PHPUnit, khung từ T03) cho:
- `Core/Validator` (T09): từng rule (required/email/min/max/match/number/unique).
- Query Builder (T05): sinh đúng SQL + params; không nối chuỗi.
- Router param-matching (T07): `/{id}` parse đúng; verb/405.
- `Common/Pagination`: offset/total pages/prev-next.
- `Services/PricingService` (T14): giá theo size.

## Files
- `tests/Unit/...`

## Done (acceptance)
- `composer test` xanh; bao phủ các nhánh chính của từng unit trên.

## Verify
Theo RULE trong [`CLAUDE.md`](../../CLAUDE.md). Bổ sung: test cả case lỗi (negative cases), không chỉ happy path.
